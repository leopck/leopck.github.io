---
title: "Transformer Architecture: A Performance Analysis of Every Component"
author: "stanley-phoong"
description: "Layer-by-layer performance analysis of the transformer architecture. We profile each component — embedding, attention, FFN, normalization — and show where compute time and memory go for models of different sizes."
publishDate: 2020-01-20
category: transformers
tags: [transformers, architecture, performance, attention, ffn, profiling]
difficulty: advanced
readingTime: 12
---

import Callout from '@/components/mdx/Callout.astro';
import PerfChart from '@/components/mdx/PerfChart.astro';
import Benchmark from '@/components/mdx/Benchmark.astro';

The transformer is a stack of identical layers, each containing multi-head attention (MHA) and a feed-forward network (FFN), connected by residual connections and layer normalization. Despite its apparent simplicity, the performance characteristics of each component differ dramatically — and the bottleneck shifts depending on model size, sequence length, and batch size.

## Component-Level Performance Breakdown

<Benchmark
  title="Time per Transformer Layer by Component (Llama-7B, A100, FP16, batch=1, seq=512)"
  columns={["Component", "FLOPs", "Time (µs)", "Share", "Bound"]}
  rows={[
    { values: ["QKV Projection", "3 × 2 × B×L × d²", "180", "18%", "Compute"], highlight: false },
    { values: ["Attention (QK^T + Softmax + AV)", "2 × 2 × B×L² × d_h × H", "220", "22%", "Compute/Memory"], highlight: true },
    { values: ["Output Projection", "2 × B×L × d²", "65", "6%", "Compute"], highlight: false },
    { values: ["FFN Gate + Up (SwiGLU)", "2 × 2 × B×L × d × d_ff", "310", "31%", "Compute"], highlight: true },
    { values: ["FFN Down", "2 × B×L × d_ff × d", "155", "15%", "Compute"], highlight: false },
    { values: ["RMSNorm (×2)", "B×L × d × 2", "25", "2.5%", "Memory BW"], highlight: false },
    { values: ["Residual Add (×2)", "B×L × d × 2", "15", "1.5%", "Memory BW"], highlight: false },
    { values: ["Other (rotary emb, etc.)", "—", "30", "3%", "—"], highlight: false },
  ]}
  notes="d=4096, d_ff=11008, H=32, d_h=128. Total: ~1000 µs per layer, 32 layers = ~32 ms per forward pass."
/>

The FFN dominates (46% of compute) because LLaMA uses SwiGLU, which has 3 weight matrices in the FFN instead of the original transformer's 2. Attention is second at 28%. Normalization and residual connections are negligible (under 5%).

<PerfChart
  title="Compute Distribution Within a Transformer Layer"
  unit="% of layer time"
  data={[
    { label: "FFN (up + gate + down)", value: 46, color: "blue", annotation: "Largest component" },
    { label: "Attention (QKV + score + output)", value: 46, color: "orange" },
    { label: "Normalization + residual", value: 5, color: "gray" },
    { label: "Other (RoPE, etc.)", value: 3, color: "gray" },
  ]}
/>

## How the Bottleneck Shifts

The dominant bottleneck changes with operating conditions:

<Benchmark
  title="Bottleneck by Operating Regime"
  columns={["Regime", "Dominant Cost", "Bound", "Optimization"]}
  rows={[
    { values: ["Prefill (large batch, long seq)", "Attention (O(N²))", "Compute", "FlashAttention, tensor cores"], highlight: true },
    { values: ["Decode (batch=1)", "Weight loading for all projections", "Memory BW", "Quantization, batching"], highlight: true },
    { values: ["Decode (batch=32)", "FFN matmul + weight loading", "Mixed", "Balance batch size with latency target"], highlight: false },
    { values: ["Very long context (32K+)", "KV cache loading", "Memory BW + capacity", "GQA, KV quantization, sliding window"], highlight: true },
  ]}
/>

<Callout type="perf" title="The N² Crossover">
  At short sequences (L under 1024), FFN dominates because its cost is O(L × d × d_ff) while attention is O(L² × d). At long sequences (L over 4096), attention's quadratic scaling overtakes FFN. The crossover point depends on the d_ff/d ratio — for LLaMA (d_ff = 2.7×d), it's around L ≈ 2.7 × d_h = ~346 tokens per head. In practice, with multi-head parallelism, attention dominates above ~2K tokens.
</Callout>

## Memory Breakdown

<Benchmark
  title="Memory Usage by Component (Llama-7B, FP16)"
  columns={["Component", "Parameters", "Memory", "Share of Model"]}
  rows={[
    { values: ["Embedding (input + output)", "2 × 32000 × 4096", "0.5 GB", "3.6%"], highlight: false },
    { values: ["QKV projections (per layer)", "3 × 4096²", "96 MB × 32 = 3.0 GB", "21.4%"], highlight: false },
    { values: ["Output projection (per layer)", "4096²", "32 MB × 32 = 1.0 GB", "7.1%"], highlight: false },
    { values: ["FFN (gate+up+down, per layer)", "(2+1) × 4096 × 11008", "258 MB × 32 = 8.1 GB", "57.9%"], highlight: true },
    { values: ["RMSNorm (per layer)", "2 × 4096", "0.5 MB × 32 = 16 MB", "0.1%"], highlight: false },
    { values: ["Total model", "6.7B parameters", "~14 GB", "100%"], highlight: false },
  ]}
  notes="FFN dominates parameter count (58%) because of the 2.7× expansion ratio in SwiGLU."
/>

FFN weights are 58% of the model. This is why weight quantization has such a large impact — quantizing FFN weights from FP16 to INT4 saves ~6 GB out of 14 GB total.

## Scaling Analysis: How Components Scale with Model Size

<Benchmark
  title="Component Scaling Across Model Sizes"
  columns={["Model", "Layers", "d_model", "Attention Share", "FFN Share", "Norm Share"]}
  rows={[
    { values: ["Llama-1B", "22", "2048", "28%", "68%", "4%"], highlight: false },
    { values: ["Llama-7B", "32", "4096", "28%", "67%", "5%"], highlight: false },
    { values: ["Llama-13B", "40", "5120", "29%", "66%", "5%"], highlight: false },
    { values: ["Llama-70B", "80", "8192", "30%", "65%", "5%"], highlight: true },
  ]}
  notes="Attention share increases slightly with model size because head count grows faster than d_ff/d ratio."
/>

The ratios are remarkably stable across model sizes. FFN consistently dominates at ~65-68%, attention at ~28-30%, and normalization is always negligible. This means optimization strategies that work for 7B generally transfer to 70B.

## Optimization Impact by Component

<PerfChart
  title="Optimization Impact by Transformer Component"
  unit="x speedup potential"
  data={[
    { label: "FFN quantization (FP16→INT4)", value: 3.0, color: "green", annotation: "Biggest win (58% of weights)" },
    { label: "FlashAttention (prefill)", value: 2.5, color: "blue", annotation: "Eliminates attention memory traffic" },
    { label: "GQA (reduce KV heads)", value: 2.0, color: "blue", annotation: "4-8× KV cache reduction" },
    { label: "Fuse norm+residual", value: 1.15, color: "orange", annotation: "Small but free" },
    { label: "Fuse QKV projection", value: 1.1, color: "orange", annotation: "3 GEMMs → 1" },
  ]}
/>

## Conclusion

The transformer's performance profile is dominated by two components: FFN (46-68% of compute/parameters) and attention (28-30%). The FFN is the primary target for weight quantization (58% of model memory). Attention is the primary target for algorithmic optimization (FlashAttention for prefill, GQA for KV cache). Normalization and residual connections are under 5% of both compute and memory — optimize them last (fuse if convenient, but don't obsess). These ratios are stable across model sizes, so optimization strategies transfer well from small to large models.
