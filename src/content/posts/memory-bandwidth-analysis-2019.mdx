---
title: "Memory Bandwidth Analysis: The Real Bottleneck in Modern Computing"
author: "stanley-phoong"
description: "Quantitative analysis of memory bandwidth across CPUs, GPUs, and accelerators. We measure achievable vs theoretical bandwidth, explain the roofline model, and show why memory bandwidth determines performance for most AI workloads."
publishDate: 2019-04-22
category: hardware-optimization
tags: [memory, bandwidth, performance, roofline, cpu, gpu, optimization]
difficulty: advanced
readingTime: 12
---

import Callout from '@/components/mdx/Callout.astro';
import PerfChart from '@/components/mdx/PerfChart.astro';
import Benchmark from '@/components/mdx/Benchmark.astro';

Memory bandwidth is the most important performance metric that most engineers don't measure. Modern processors can compute far faster than they can feed data — a CPU can perform 1 TFLOP/s but its DRAM delivers only ~50 GB/s, meaning most operations wait for data. Understanding where you sit on the compute-memory spectrum is the first step to optimization.

## The Bandwidth Landscape

<Benchmark
  title="Memory Bandwidth Across Platforms (2019-2024)"
  columns={["Platform", "Memory Type", "Theoretical BW", "Achieved BW", "Efficiency"]}
  rows={[
    { values: ["Intel Xeon 8280 (2 socket)", "DDR4-2933", "281 GB/s", "~210 GB/s", "75%"], highlight: false },
    { values: ["AMD EPYC 7742 (2 socket)", "DDR4-3200", "410 GB/s", "~340 GB/s", "83%"], highlight: false },
    { values: ["NVIDIA V100", "HBM2", "900 GB/s", "~820 GB/s", "91%"], highlight: true },
    { values: ["NVIDIA A100", "HBM2e", "2,039 GB/s", "~1,800 GB/s", "88%"], highlight: true },
    { values: ["NVIDIA H100 (SXM)", "HBM3", "3,350 GB/s", "~2,900 GB/s", "87%"], highlight: true },
    { values: ["Intel Gaudi2", "HBM2e", "2,450 GB/s", "~2,200 GB/s", "90%"], highlight: true },
    { values: ["Apple M2 Ultra", "LPDDR5", "800 GB/s", "~680 GB/s", "85%"], highlight: false },
  ]}
  notes="Achieved BW measured with STREAM triad benchmark (or equivalent). HBM consistently achieves 85-91% of theoretical."
/>

The GPU advantage is clear: HBM delivers 5-15× more bandwidth than DDR. This is why GPUs dominate AI workloads — not because of raw compute (which CPUs have narrowed the gap on), but because of memory bandwidth.

## The Roofline Model

The roofline model tells you whether an operation is compute-bound or memory-bound based on its **arithmetic intensity** (AI): the ratio of FLOPs to bytes transferred.

**AI = FLOPs / Bytes**

The crossover point where compute and memory bandwidth are balanced:

**AI_crossover = Peak_FLOPS / Peak_BW**

<Benchmark
  title="Roofline Crossover Points"
  columns={["Platform", "Peak FP16 FLOPS", "Peak BW", "Crossover AI", "Interpretation"]}
  rows={[
    { values: ["V100", "125 TFLOPS", "900 GB/s", "139 FLOP/byte", "Most ops are memory-bound"], highlight: false },
    { values: ["A100", "312 TFLOPS", "2,039 GB/s", "153 FLOP/byte", "Even higher AI needed"], highlight: true },
    { values: ["H100", "990 TFLOPS", "3,350 GB/s", "295 FLOP/byte", "Almost everything is memory-bound"], highlight: true },
  ]}
  notes="As compute grows faster than bandwidth (each generation), the crossover AI increases — more operations become memory-bound."
/>

<Callout type="perf" title="The Trend Is Clear">
  Each GPU generation increases compute faster than bandwidth. V100 needs AI > 139 to be compute-bound; H100 needs AI over 295. This means more and more operations are memory-bound on newer hardware — making bandwidth optimization increasingly important.
</Callout>

## Measuring Actual Bandwidth

### STREAM Benchmark

The gold standard for measuring achievable memory bandwidth:

```bash
# Build and run STREAM (adjust array size for your cache)
gcc -O3 -fopenmp -DSTREAM_ARRAY_SIZE=100000000 stream.c -o stream
OMP_NUM_THREADS=48 ./stream
```

The four STREAM operations:
- **Copy:** `a[i] = b[i]` — pure read + write bandwidth
- **Scale:** `a[i] = q * b[i]` — adds one multiply
- **Add:** `a[i] = b[i] + c[i]` — two reads, one write
- **Triad:** `a[i] = b[i] + q * c[i]` — the standard benchmark (AI = 0.33 FLOP/byte)

### GPU Bandwidth

```bash
# NVIDIA's bandwidth test
/usr/local/cuda/samples/1_Utilities/bandwidthTest/bandwidthTest
# Or use custom kernel with nvprof/ncu
ncu --metrics dram__bytes_read.sum,dram__bytes_write.sum ./my_kernel
```

## Where AI Workloads Fall on the Roofline

<PerfChart
  title="Arithmetic Intensity of Common AI Operations"
  unit="FLOP/byte"
  data={[
    { label: "LLM decode (batch=1)", value: 0.5, color: "red", annotation: "Deep in memory-bound" },
    { label: "LLM decode (batch=32)", value: 16, color: "orange", annotation: "Still memory-bound" },
    { label: "Standard attention (seq=4K)", value: 3, color: "red" },
    { label: "FlashAttention (seq=4K)", value: 89, color: "blue", annotation: "Near crossover" },
    { label: "LLM prefill (batch=32)", value: 250, color: "green", annotation: "Compute-bound" },
    { label: "GEMM (4096×4096)", value: 340, color: "green", annotation: "Compute-bound" },
  ]}
/>

Most LLM operations are memory-bound, especially during the decode phase. Only large batched prefill and big GEMM operations cross into compute-bound territory. This is the fundamental reason why LLM serving focuses so heavily on memory optimization.

## Bandwidth Optimization Techniques

<Benchmark
  title="Bandwidth Optimization Techniques"
  columns={["Technique", "Applicable When", "Bandwidth Improvement", "Mechanism"]}
  rows={[
    { values: ["Quantization (FP16→INT4)", "Weight loading", "2-4×", "Fewer bytes to read"], highlight: true },
    { values: ["Batching", "Decode phase", "Up to 64×", "Amortize weight reads"], highlight: true },
    { values: ["Operator fusion", "Multi-op sequences", "1.5-3×", "Eliminate intermediate writes"], highlight: true },
    { values: ["Data layout (SoA)", "Strided access", "2-10×", "Better coalescing/prefetch"], highlight: false },
    { values: ["Tiling (SMEM)", "Reusable data", "3-15×", "Serve from fast SRAM instead"], highlight: false },
    { values: ["Compression (sparse)", "Sparse models", "1.5-4×", "Skip zero values"], highlight: false },
  ]}
/>

<Callout type="tip" title="The Bandwidth Budget">
  Before optimizing any kernel, calculate its bandwidth requirement: `bytes_read + bytes_written`. Divide by your GPU's achieved bandwidth (~90% of theoretical for HBM). If the result exceeds your kernel's runtime, the kernel is not yet bandwidth-limited — look for other bottlenecks. If it's close, you're hitting the hardware limit and need algorithmic changes (quantization, fusion, tiling).
</Callout>

## Multi-Level Bandwidth Optimization

The most effective optimizations exploit the bandwidth gap between memory levels:

<PerfChart
  title="Bandwidth Reduction Through Memory Hierarchy Exploitation (A100)"
  unit="GB/s needed from HBM"
  data={[
    { label: "Naive (all from HBM)", value: 2000, color: "red", annotation: "Exceeds HBM BW" },
    { label: "+ L2 cache hits (40%)", value: 1200, color: "orange" },
    { label: "+ SMEM tiling", value: 400, color: "blue" },
    { label: "+ Operator fusion", value: 200, color: "green", annotation: "Within HBM budget" },
  ]}
/>

Each level of optimization reduces the demand on HBM. The combination of caching, tiling, and fusion can reduce HBM traffic by 10× — often the difference between a kernel being bandwidth-limited and performing at acceptable throughput.

## Conclusion

Memory bandwidth is the primary bottleneck for most AI workloads on modern hardware. GPU HBM delivers 5-15× more bandwidth than CPU DDR, which is why GPUs dominate AI — but even HBM bandwidth is insufficient for operations like LLM decode at batch=1. The roofline model provides the analytical framework: calculate your operation's arithmetic intensity, compare to the platform's crossover point, and optimize accordingly. For memory-bound operations (most of LLM serving), the optimization hierarchy is: batch more, quantize weights, fuse operators, tile into SRAM.
