---
title: "Speculative Decoding: Trading Compute for Latency in LLM Inference"
author: "stanley-phoong"
description: "How speculative decoding uses a small draft model to propose multiple tokens that the large model verifies in parallel — achieving 2-3× decode speedup without any quality loss. We analyze the acceptance rate math and when speculation helps."
publishDate: 2020-10-15
category: llm-inference
tags: [speculative-decoding, llm, inference, latency, optimization, performance]
difficulty: advanced
readingTime: 12
---

import Callout from '@/components/mdx/Callout.astro';
import PerfChart from '@/components/mdx/PerfChart.astro';
import Benchmark from '@/components/mdx/Benchmark.astro';

LLM decode generates one token per forward pass — each requiring a full model weight read from HBM. For a 70B model, that's 140 GB of weight loading per token, taking ~7 ms on an A100 at batch=1. Speculative decoding breaks this 1-token-per-pass barrier: a small draft model proposes K tokens cheaply, the large model verifies all K in a single forward pass, and accepted tokens skip individual decode steps.

## The Core Idea

Standard decode: 1 forward pass → 1 token → 7 ms per token.
Speculative decode: 1 draft pass (~0.5 ms) + 1 verification pass (~8 ms) → accept 2-4 tokens → 2.8-4.3 ms per token.

<Benchmark
  title="Speculative Decoding Mechanics"
  columns={["Step", "Model Used", "Cost", "Output"]}
  rows={[
    { values: ["1. Draft K tokens", "Small model (1-2B)", "~0.5 ms per token", "K candidate tokens"], highlight: false },
    { values: ["2. Verify all K in parallel", "Large model (70B)", "~8 ms (1 forward pass)", "Accept first N ≤ K + generate 1 bonus token"], highlight: true },
    { values: ["3. Output accepted tokens", "—", "0 ms", "N+1 tokens total"], highlight: false },
  ]}
  notes="Verification is a single forward pass because the large model processes all K positions in parallel (like prefill)."
/>

The key insight: the verification forward pass costs about the same as generating 1 token (both load the same model weights), but it produces N+1 tokens. The effective speedup is **(N+1) / (K × t_draft + t_verify)**.

<Callout type="perf" title="Why This Is Free Quality">
  Speculative decoding is mathematically guaranteed to produce the same distribution as standard sampling from the large model. Accepted tokens match exactly. Rejected tokens are resampled from a corrected distribution. There is zero quality loss — it's a pure latency optimization.
</Callout>

## Acceptance Rate: The Critical Metric

The speedup depends on how many draft tokens the large model accepts:

<Benchmark
  title="Acceptance Rate by Task and Draft Model Quality"
  columns={["Task", "Draft Model", "Target Model", "Avg Accepted (K=5)", "Effective Speedup"]}
  rows={[
    { values: ["Code generation", "CodeLlama-7B", "CodeLlama-70B", "3.8 of 5", "2.7×"], highlight: true },
    { values: ["Chat (general)", "Llama-7B", "Llama-70B", "2.5 of 5", "2.0×"], highlight: false },
    { values: ["Creative writing", "Llama-7B", "Llama-70B", "1.8 of 5", "1.6×"], highlight: false },
    { values: ["Factual QA", "Llama-7B", "Llama-70B", "3.2 of 5", "2.4×"], highlight: true },
    { values: ["Translation", "NLLB-600M", "NLLB-3.3B", "3.5 of 5", "2.6×"], highlight: false },
  ]}
  notes="Higher acceptance rates on predictable tasks (code, QA). Lower on creative tasks where the draft model diverges."
/>

<PerfChart
  title="Effective Tokens per Second (Llama-70B, A100, batch=1)"
  unit="tok/s"
  data={[
    { label: "Standard decode", value: 14, color: "gray" },
    { label: "Speculative (K=3, code)", value: 38, color: "green", annotation: "2.7×" },
    { label: "Speculative (K=5, code)", value: 36, color: "green", annotation: "2.6× (K=5 has more waste)" },
    { label: "Speculative (K=3, chat)", value: 28, color: "blue", annotation: "2.0×" },
    { label: "Speculative (K=5, creative)", value: 22, color: "orange", annotation: "1.6×" },
  ]}
/>

## The Math: Optimal K

The expected tokens per speculation round is E[accepted] + 1. The cost per round is K × t_draft + t_verify. The optimal K depends on acceptance probability α:

**Speedup = (E[accepted] + 1) / (K × t_draft/t_verify + 1)**

Where E[accepted] = (1 - α^(K+1)) / (1 - α) for geometric acceptance.

<Benchmark
  title="Optimal K by Acceptance Rate"
  columns={["Acceptance Rate (α)", "Optimal K", "Expected Accepted", "Speedup (t_draft/t_verify = 0.07)"]}
  rows={[
    { values: ["0.9 (very aligned)", "7-8", "6.5", "3.8×"], highlight: true },
    { values: ["0.8 (well aligned)", "5-6", "4.0", "2.8×"], highlight: true },
    { values: ["0.7 (moderate)", "3-4", "2.7", "2.1×"], highlight: false },
    { values: ["0.5 (poor alignment)", "2-3", "1.5", "1.4×"], highlight: false },
    { values: ["0.3 (misaligned)", "1-2", "0.8", "1.1× (barely helps)"], highlight: false },
  ]}
  notes="When α < 0.4, speculation overhead exceeds benefit. Use standard decode instead."
/>

<Callout type="info" title="Draft Model Quality Matters Enormously">
  The draft model must approximate the target model's distribution well. A 7B draft for a 70B target works because they share vocabulary and training data distribution. A completely different architecture (e.g., LSTM draft for Transformer target) would have α < 0.3 — not worth speculating.
</Callout>

## Variants

**Self-speculative decoding (Medusa, EAGLE):** Instead of a separate draft model, add small prediction heads to the target model itself. Each head predicts N-ahead tokens from the last hidden state. Eliminates the need for a separate draft model but adds ~5% parameters.

**Prompt lookup decoding:** For tasks with repetitive output (structured data, templates), look up candidate tokens from the prompt or a cache. Zero draft cost but only works when output resembles existing text.

**Tree speculation:** Instead of a single draft sequence, generate a tree of candidates. Verify the entire tree in one forward pass. Accepts more tokens per round at the cost of longer verification.

<Benchmark
  title="Speculation Variant Comparison"
  columns={["Variant", "Draft Cost", "Acceptance Rate", "Complexity", "Best For"]}
  rows={[
    { values: ["Standard (separate model)", "K × t_draft", "High (if models aligned)", "Medium", "General purpose"], highlight: true },
    { values: ["Medusa (self-draft heads)", "~0 extra", "Moderate", "Low (single model)", "When no good draft model exists"], highlight: true },
    { values: ["EAGLE (feature-based)", "~0.05 × t_verify", "High", "Medium", "Best single-model method"], highlight: true },
    { values: ["Prompt lookup", "~0", "Task-dependent", "Low", "Repetitive/structured output"], highlight: false },
    { values: ["Tree speculation", "K × branches × t_draft", "Highest (tree covers more)", "High", "Batch=1 latency-critical"], highlight: false },
  ]}
/>

## When Speculation Doesn't Help

<Benchmark
  title="When to Skip Speculative Decoding"
  columns={["Scenario", "Why Speculation Fails", "Better Alternative"]}
  rows={[
    { values: ["Batch size > 8", "Target model already bandwidth-saturated", "Standard decode (already efficient)"], highlight: true },
    { values: ["Very short outputs (< 10 tokens)", "Setup overhead exceeds savings", "Standard decode"], highlight: false },
    { values: ["No good draft model available", "Low acceptance rate (α < 0.4)", "Medusa/EAGLE heads instead"], highlight: false },
    { values: ["Streaming (TTFT-sensitive)", "Draft adds latency to first token", "Standard decode for first chunk, then speculate"], highlight: false },
    { values: ["Memory-constrained", "Draft model consumes additional VRAM", "Quantize both models"], highlight: true },
  ]}
/>

<Callout type="warning" title="Batch Size Is the Key Variable">
  Speculative decoding helps most at batch=1 (pure memory-latency-bound decode). As batch size increases, the target model forward pass already amortizes weight loading across more tokens — the marginal benefit of speculation shrinks. At batch=16+, standard decode is often faster because the speculation overhead (running the draft model) exceeds the savings.
</Callout>

## Conclusion

Speculative decoding delivers 2-3× latency reduction for LLM decode at small batch sizes, with zero quality loss. The speedup is proportional to the draft-target acceptance rate, which depends on model alignment and task predictability. Use K=3-5 draft tokens with a well-aligned smaller model (same family, 7B draft for 70B target). For scenarios without a good draft model, self-speculative methods (Medusa, EAGLE) add prediction heads to the target model itself. The technique is most impactful at batch=1 and loses benefit as batch size increases beyond 8.
