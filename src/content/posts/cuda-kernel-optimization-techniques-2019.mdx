---
title: "CUDA Kernel Optimization Techniques: A Systematic Approach"
author: "stanley-phoong"
description: "A practitioner's guide to CUDA kernel optimization. We start from Nsight Compute profiler output and systematically address the most common bottlenecks: memory coalescing, occupancy, instruction mix, and launch overhead."
publishDate: 2019-04-15
category: gpu-programming
tags: [cuda, kernel, optimization, performance, profiling, nsight]
difficulty: advanced
readingTime: 14
---

import Callout from '@/components/mdx/Callout.astro';
import PerfChart from '@/components/mdx/PerfChart.astro';
import Benchmark from '@/components/mdx/Benchmark.astro';

CUDA kernel optimization is not guesswork — it's a systematic process driven by profiler data. The Nsight Compute profiler tells you exactly what's limiting performance: memory bandwidth, compute throughput, latency, or launch overhead. This post walks through the optimization workflow from profiling to fix.

## The Optimization Workflow

The process is always the same: **profile → identify bottleneck → fix → reprofile**. Never optimize blind.

```bash
# Step 1: Profile the kernel
ncu --set full -o report ./my_application

# Step 2: Look at the Speed of Light (SOL) section
# It tells you: % of peak compute, % of peak memory bandwidth
# Whichever is higher is your bottleneck
```

<Benchmark
  title="Speed of Light Interpretation"
  columns={["SOL Compute", "SOL Memory", "Bottleneck", "Optimization Path"]}
  rows={[
    { values: ["over 60%", "under 40%", "Compute-bound", "Reduce instructions, use specialized units (tensor cores)"], highlight: false },
    { values: ["under 40%", "over 60%", "Memory-bound", "Improve coalescing, add caching/tiling, reduce traffic"], highlight: true },
    { values: ["under 40%", "under 40%", "Latency-bound", "Increase occupancy, overlap compute and memory"], highlight: true },
    { values: ["over 60%", "over 60%", "Well-balanced", "Both resources utilized — minor tuning only"], highlight: false },
  ]}
  notes="Most AI kernels are memory-bound or latency-bound. Compute-bound is the goal but rare outside of large GEMM."
/>

## Fix #1: Memory Coalescing (Biggest Impact)

The single most common problem. Non-coalesced global memory access wastes 4-32× bandwidth:

```cpp
// BAD: Column-major access in row-major array — stride = N per thread
__global__ void bad_coalescing(float *matrix, float *output, int N) {
    int row = threadIdx.x;  // Adjacent threads access different rows
    int col = blockIdx.x;
    output[row * N + col] = matrix[row * N + col] * 2.0f;
    // Thread 0: matrix[0], Thread 1: matrix[N], Thread 2: matrix[2N]
    // Stride = N → non-coalesced!
}

// GOOD: Row-major access — stride = 1 per thread
__global__ void good_coalescing(float *matrix, float *output, int N) {
    int col = threadIdx.x;  // Adjacent threads access adjacent columns
    int row = blockIdx.x;
    output[row * N + col] = matrix[row * N + col] * 2.0f;
    // Thread 0: matrix[row*N], Thread 1: matrix[row*N+1], ...
    // Stride = 1 → perfectly coalesced!
}
```

<PerfChart
  title="Impact of Memory Coalescing Fix"
  unit="GB/s achieved bandwidth"
  data={[
    { label: "Non-coalesced (stride=N)", value: 45, color: "red", annotation: "5% of peak" },
    { label: "Partially coalesced (stride=2)", value: 420, color: "orange" },
    { label: "Fully coalesced (stride=1)", value: 830, color: "green", annotation: "92% of peak" },
  ]}
/>

<Callout type="perf" title="How to Detect">
  In Nsight Compute, check `l1tex__t_sectors_pipe_lsu_mem_global_op_ld.sum` vs `l1tex__t_requests_pipe_lsu_mem_global_op_ld.sum`. If sectors/request >over 1, you have coalescing issues. Ideal is 1 sector per request (32 threads × 4 bytes = 128 bytes = 1 sector).
</Callout>

## Fix #2: Occupancy and Latency Hiding

Low occupancy means the GPU can't hide memory latency by switching between warps:

<Benchmark
  title="Common Occupancy Limiters"
  columns={["Limiter", "How to Detect", "Fix"]}
  rows={[
    { values: ["Too many registers", "ncu: Registers per thread over 64", "Use __launch_bounds__, reduce local variables"], highlight: true },
    { values: ["Too much shared memory", "ncu: Shared memory over 48KB/block", "Reduce tile size, use multi-phase tiling"], highlight: true },
    { values: ["Block size too large", "Block dim over 512 with high resources", "Try 128 or 256 threads per block"], highlight: false },
    { values: ["Block size too small", "Block dim under 128", "Increase to at least 128 for latency hiding"], highlight: false },
  ]}
/>

```cpp
// Limit register usage to improve occupancy
__global__ __launch_bounds__(256, 4)  // 256 threads, at least 4 blocks/SM
void optimized_kernel(float *data, int n) {
    // Compiler will spill registers to local memory if needed
    // to maintain the 4-blocks-per-SM target
}
```

## Fix #3: Kernel Fusion (Eliminate Launch Overhead)

Each CUDA kernel launch costs ~5-10 µs. For small kernels, this overhead dominates:

<Benchmark
  title="Kernel Launch Overhead Analysis"
  columns={["Kernel", "Compute Time", "Launch Overhead", "Overhead %", "Action"]}
  rows={[
    { values: ["Large GEMM", "2.0 ms", "5 µs", "0.25%", "No action needed"], highlight: false },
    { values: ["LayerNorm", "50 µs", "5 µs", "10%", "Fuse with adjacent ops"], highlight: true },
    { values: ["Bias add", "8 µs", "5 µs", "38%", "Must fuse — overhead dominates"], highlight: true },
    { values: ["Activation (GELU)", "12 µs", "5 µs", "29%", "Fuse with preceding GEMM"], highlight: true },
  ]}
/>

The solution: fuse small element-wise operations into the preceding or following GEMM kernel, or combine sequential element-wise ops into a single kernel:

```cpp
// Unfused: 3 launches, 3 HBM round-trips
bias_add_kernel<<<grid, block>>>(output, bias, n);        // 8 µs compute + 5 µs launch
gelu_kernel<<<grid, block>>>(output, n);                  // 12 µs compute + 5 µs launch  
residual_add_kernel<<<grid, block>>>(output, residual, n); // 8 µs compute + 5 µs launch
// Total: 43 µs

// Fused: 1 launch, 1 HBM round-trip
fused_bias_gelu_residual<<<grid, block>well above(output, bias, residual, n);
// Total: 16 µs (2.7× faster)
```

## Fix #4: Instruction Mix Optimization

For compute-bound kernels, the instruction mix matters. Special function units (SFU) for transcendentals are limited:

<Benchmark
  title="Instruction Throughput (per SM per clock, V100)"
  columns={["Operation", "Unit", "Throughput", "Notes"]}
  rows={[
    { values: ["FP32 FMA", "CUDA cores", "64 ops/clock", "Highest throughput"], highlight: true },
    { values: ["FP16 FMA (tensor)", "Tensor cores", "~1024 ops/clock", "8× FP32 throughput"], highlight: true },
    { values: ["INT32", "INT units", "64 ops/clock", "Same as FP32"], highlight: false },
    { values: ["exp/log/sin/cos", "SFU (special function)", "16 ops/clock", "4× slower than FMA"], highlight: false },
    { values: ["Division", "SFU", "16 ops/clock", "Use reciprocal + multiply instead"], highlight: false },
  ]}
/>

```cpp
// Slow: uses SFU for division (16 ops/clock)
float result = a / b;

// Fast: reciprocal approximation + multiply (64 ops/clock)
float result = a * __frcp_rn(b);  // 1-ULP accurate reciprocal

// Slow: expf uses SFU
float result = expf(x);

// Faster for approximation: use __expf (less accurate but faster)
float result = __expf(x);  // Fast math intrinsic
```

<Callout type="warning" title="Fast Math Trade-offs">
  `__expf`, `__logf`, `__sinf` etc. are faster but have reduced precision (~2 ULP vs 1 ULP). For softmax denominators and loss computation, use the standard versions. For intermediate activations where 2-ULP error is acceptable, fast math intrinsics are free performance.
</Callout>

## Fix #5: Avoid Warp Divergence

When threads within a warp take different branches, both paths execute serially:

```cpp
// BAD: 50% of threads idle in each branch (warp divergence)
if (threadIdx.x % 2 == 0) {
    expensive_path_A(data);  // Half the warp
} else {
    expensive_path_B(data);  // Other half
}

// BETTER: Reorganize so entire warps take the same branch
int warp_id = threadIdx.x / 32;
if (warp_id % 2 == 0) {
    expensive_path_A(data);  // Entire warp A
} else {
    expensive_path_B(data);  // Entire warp B — no divergence!
}
```

## The Optimization Priority Checklist

<PerfChart
  title="Typical Impact by Optimization (ordered by expected speedup)"
  unit="x improvement"
  data={[
    { label: "Fix memory coalescing", value: 8, color: "red", annotation: "Fix first — dominates" },
    { label: "Add shared memory tiling", value: 4, color: "orange" },
    { label: "Fuse small kernels", value: 2.5, color: "blue" },
    { label: "Fix occupancy", value: 1.5, color: "blue" },
    { label: "Use tensor cores", value: 1.5, color: "green", annotation: "If applicable" },
    { label: "Fast math intrinsics", value: 1.2, color: "green" },
    { label: "Reduce divergence", value: 1.1, color: "gray" },
  ]}
/>

Always fix coalescing first. Then add tiling if there's data reuse. Then fuse small kernels. Only after these should you worry about occupancy, instruction mix, and divergence.

## Conclusion

CUDA optimization is a systematic process: profile with Nsight Compute, read the Speed of Light metrics to identify compute-bound vs memory-bound, then apply fixes in priority order. Memory coalescing and tiling typically deliver 4-8× improvement. Kernel fusion eliminates launch overhead for small operations. Occupancy tuning and instruction optimization provide the final 20-50%. Never optimize without profiler data — intuition about GPU performance is almost always wrong.
