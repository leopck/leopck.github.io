# Jekyll site dependencies - use flexible versioning to avoid circular dependencies
source "https://rubygems.org"

# Use Jekyll with more flexible versioning
gem "jekyll"

# Allow bundler to resolve dependencies naturally
# Development tools
group :development do
  gem "rake"
end