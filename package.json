{
  "name": "fridays-with-faraday-site",
  "version": "1.0.0",
  "description": "Static site for Fridays with Faraday blog",
  "scripts": {
    "build": "npm run clean && node generator-enhanced.js",
    "build:prod": "npm run build",
    "dev": "npm run build && npx http-server output -p 3000 -o",
    "clean": "rm -rf dist output",
    "build:clean": "npm run clean && npm run build",
    "test": "npm run build && echo '✅ Build test passed'",
    "serve": "npx http-server output -p 3000 -o",
    "prebuild": "echo '🚀 Starting build process...'",
    "postbuild": "echo '✅ Build completed successfully!'"
  },
  "keywords": [
    "static-site",
    "blog",
    "github-pages"
  ],
  "author": "Fridays with Faraday",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
